# 電商營運計算助手

原生 HTML、CSS、JavaScript 製作的手機優先 PWA，無後端、無需安裝相依套件。

## 本機預覽

在本目錄啟動靜態檔案伺服器後，以瀏覽器開啟。Service Worker 必須透過 `localhost` 或 HTTPS 使用。

## GitHub Pages

將本目錄內容放到儲存庫根目錄，並在 GitHub Pages 選擇由分支部署。所有資源皆採相對路徑，可支援專案子路徑。
